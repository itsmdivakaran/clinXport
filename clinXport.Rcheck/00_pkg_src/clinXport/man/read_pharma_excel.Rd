% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_excel.R
\name{read_pharma_excel}
\alias{read_pharma_excel}
\title{Read an Excel file with clinical metadata support}
\usage{
read_pharma_excel(
  path,
  sheet = 1L,
  spec_sheet = NULL,
  col_types = NULL,
  label_row = NULL,
  na = c("", "NA", ".", " "),
  skip = 0L,
  ...
)
}
\arguments{
\item{path}{Character. Path to the \code{.xlsx} / \code{.xls} file.}

\item{sheet}{Sheet name or index. Default \code{1} (first sheet).}

\item{spec_sheet}{Character. Name of an optional sheet inside the workbook
containing a variable specification (columns: \code{variable}, \code{label},
\code{format}, \code{length}, \code{type}). Set to \code{NULL} (default) to skip.}

\item{col_types}{Named character vector overriding column types. Passed to
\code{\link[readxl:read_excel]{readxl::read_excel()}}. Use \code{"text"}, \code{"numeric"}, \code{"date"}, \code{"logical"}.}

\item{label_row}{Logical or \code{NULL}. If \code{TRUE}, treat the first row after the
column names as variable labels and remove it from the data. If \code{NULL}
(default), this is detected when the row matches labels in \code{spec_sheet}.}

\item{na}{Character vector of strings to treat as \code{NA}. Default
\code{c("", "NA", ".", " ")}.}

\item{skip}{Integer. Number of rows to skip before reading data. Default 0.}

\item{...}{Additional arguments passed to \code{\link[readxl:read_excel]{readxl::read_excel()}}.}
}
\value{
A \code{data.frame} (not a tibble). If a spec sheet is found, variable
labels are attached via \code{\link[=set_var_label]{set_var_label()}}.
}
\description{
Reads an \code{.xlsx} file using \code{\link[readxl:read_excel]{readxl::read_excel()}} and optionally converts
date columns, coerces types, and restores variable labels from a companion
spec sheet within the same workbook.
}
\examples{
\dontrun{
adsl <- read_pharma_excel("adsl.xlsx", sheet = "ADSL")
}

}
\seealso{
\code{\link[=write_pharma_excel]{write_pharma_excel()}}, \code{\link[=read_xpt]{read_xpt()}}, \code{\link[=apply_spec]{apply_spec()}}
}
