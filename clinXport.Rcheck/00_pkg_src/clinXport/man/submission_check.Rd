% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validate.R
\name{submission_check}
\alias{submission_check}
\title{Run a full submission-readiness check on a dataset}
\usage{
submission_check(
  df,
  domain = NULL,
  xpt_version = 5L,
  check_labels = TRUE,
  check_formats = TRUE,
  check_lengths = TRUE,
  verbose = TRUE
)
}
\arguments{
\item{df}{A \code{data.frame}.}

\item{domain}{Character. CDISC domain code (e.g. \code{"DM"}, \code{"AE"}).
Pass \code{NULL} to skip domain-specific checks.}

\item{xpt_version}{Integer. XPT version used for naming rules: \code{5} or \code{8}.
Default \code{5}.}

\item{check_labels}{Logical. Warn on missing labels. Default \code{TRUE}.}

\item{check_formats}{Logical. Warn on missing formats for date columns.
Default \code{TRUE}.}

\item{check_lengths}{Logical. Error if any character variable exceeds the
200-byte XPT v5 limit. Default \code{TRUE}.}

\item{verbose}{Logical. Print the report to the console. Default \code{TRUE}.}
}
\value{
A \code{data.frame} (invisibly) with columns \code{level} (\code{"ERROR"},
\code{"WARNING"}, \code{"INFO"}), \code{variable}, \code{check}, \code{message}.
}
\description{
Performs a battery of checks relevant to FDA eCTD / PMDA submissions:
XPT v5 naming rules, variable label completeness, format string validity,
character length limits, numeric precision, and CDISC domain conventions
(STUDYID, DOMAIN, USUBJID presence, etc.).
}
\examples{
\dontrun{
dm <- read_xpt("dm.xpt")
report <- submission_check(dm, domain = "DM")
}

}
\seealso{
\code{\link[=check_xpt_names]{check_xpt_names()}}, \code{\link[=check_var_lengths]{check_var_lengths()}}
}
