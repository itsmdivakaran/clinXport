% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/write_xpt.R
\name{write_xpt}
\alias{write_xpt}
\title{Write a data frame to SAS XPT format}
\usage{
write_xpt(
  df,
  path,
  version = 5L,
  dataset_name = NULL,
  dataset_label = "",
  var_labels = NULL,
  var_formats = NULL,
  var_lengths = NULL,
  sanitize_names = TRUE
)
}
\arguments{
\item{df}{A \code{data.frame} to export.}

\item{path}{Character. Output file path (\code{.xpt}).}

\item{version}{Integer. XPT version: \code{5} (default) or \code{8}.
Version 5 restricts column names to <= 8 uppercase alphanumeric characters.
Version 8 allows up to 32 characters.}

\item{dataset_name}{Character. SAS dataset name (<= 8 chars for v5). If not
supplied, the object name of \code{df} is used (truncated as needed).}

\item{dataset_label}{Character. Dataset label (<= 40 chars). Default \code{""}.}

\item{var_labels}{Named character vector overriding variable labels.
Falls back to \code{attr(df, "var_labels")} then \code{attr(col, "label")}.}

\item{var_formats}{Named character vector overriding SAS format strings
(e.g. \code{c(RFSTDTC = "DATE9.", AGE = "3.")}).
Falls back to \code{attr(df, "var_formats")}.}

\item{var_lengths}{Named integer vector overriding character column byte
lengths. Numeric columns are always 8 bytes. Falls back to
\code{attr(df, "var_lengths")}.}

\item{sanitize_names}{Logical. If \code{TRUE} (default), column names are
automatically sanitised for XPT compliance (uppercase, <= 8/32 chars,
alphanumeric + underscore only).}
}
\value{
Invisibly returns \code{path}.
}
\description{
Exports a data frame to SAS Transport format (XPORT engine), version 5 or 8.
Variable metadata (labels, formats, lengths) can be passed directly or
inferred from the data frame's own attributes (as set by \code{\link[=set_var_label]{set_var_label()}},
\code{\link[=set_var_format]{set_var_format()}}, and \code{\link[=set_var_length]{set_var_length()}}).
}
\examples{
\dontrun{
df <- data.frame(USUBJID = "STUDY-001-001", AGE = 34L, SEX = "M")
df <- set_var_label(df, c(USUBJID = "Unique Subject Identifier",
                          AGE = "Age at Screening",
                          SEX = "Sex"))
df <- set_var_format(df, c(AGE = "3."))
write_xpt(df, "dm.xpt", dataset_name = "DM", dataset_label = "Demographics")
}

}
\seealso{
\code{\link[=read_xpt]{read_xpt()}}, \code{\link[=set_var_label]{set_var_label()}}, \code{\link[=set_var_format]{set_var_format()}}, \code{\link[=apply_spec]{apply_spec()}}
}
