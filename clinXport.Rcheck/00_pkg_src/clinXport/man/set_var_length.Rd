% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/metadata.R
\name{set_var_length}
\alias{set_var_length}
\title{Set variable byte lengths on a data frame}
\usage{
set_var_length(df, lengths)
}
\arguments{
\item{df}{A \code{data.frame}.}

\item{lengths}{Named integer vector of byte lengths (e.g.
\code{c(USUBJID = 20L, RACE = 200L)}).}
}
\value{
The modified \code{data.frame} (invisibly).
}
\description{
Used to control the width written to XPT/SAS7BDAT for character columns.
Numeric columns are always 8 bytes in XPT and are silently unaffected.
}
\examples{
df <- data.frame(USUBJID = "001-001", RACE = "WHITE")
df <- set_var_length(df, c(USUBJID = 20L, RACE = 200L))
get_var_length(df)

}
