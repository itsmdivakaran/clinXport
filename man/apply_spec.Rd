% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spec.R
\name{apply_spec}
\alias{apply_spec}
\title{Apply a variable specification to a data frame}
\usage{
apply_spec(
  df,
  spec,
  coerce_types = TRUE,
  reorder = FALSE,
  select_only = FALSE,
  quiet = FALSE
)
}
\arguments{
\item{df}{A \code{data.frame}.}

\item{spec}{A spec \code{data.frame} as returned by \code{\link[=import_spec]{import_spec()}}, or any
\code{data.frame} with at least a \code{variable} column.}

\item{coerce_types}{Logical. If \code{TRUE}, coerce column types to match the
\code{type} column of the spec (\code{"Char"} -> \code{character}, \code{"Num"} ->
\code{numeric}). Default \code{TRUE}.}

\item{reorder}{Logical. If \code{TRUE}, reorder \code{df} columns to match spec order.
Columns in \code{df} but not in the spec are appended at the end. Default
\code{FALSE}.}

\item{select_only}{Logical. If \code{TRUE} and \code{reorder = TRUE}, only columns
present in the spec are returned. Default \code{FALSE}.}

\item{quiet}{Logical. Suppress informational messages. Default \code{FALSE}.}
}
\value{
The modified \code{data.frame}.
}
\description{
Applies metadata from a spec table to a data frame: attaches labels, SAS
format strings, and byte lengths; optionally coerces column types; optionally
reorders or selects columns to match the spec order.
}
\examples{
\dontrun{
spec <- import_spec("dm_spec.xlsx")
dm   <- apply_spec(dm_raw, spec, coerce_types = TRUE, reorder = TRUE)
}

}
\seealso{
\code{\link[=import_spec]{import_spec()}}, \code{\link[=set_var_label]{set_var_label()}}, \code{\link[=set_var_format]{set_var_format()}}
}
