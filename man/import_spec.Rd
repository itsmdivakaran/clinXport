% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spec.R
\name{import_spec}
\alias{import_spec}
\title{Import a variable specification from a file}
\usage{
import_spec(path, sheet = 1L, dataset = NULL)
}
\arguments{
\item{path}{Character. Path to the spec file (\code{.xlsx}, \code{.xls}, or \code{.csv}).}

\item{sheet}{Sheet name or index for Excel files. Default \code{1}.}

\item{dataset}{Character. If the spec covers multiple datasets, filter to
this dataset name. If \code{NULL}, all rows are returned.}
}
\value{
A standardised spec \code{data.frame}.
}
\description{
Reads a specification table from an Excel or CSV file and returns a
standardised \code{data.frame} that can be passed to \code{\link[=apply_spec]{apply_spec()}}.
}
\details{
The spec file must contain at least a \code{variable} column. All other columns
are optional but recognised:
\describe{
\item{\code{variable}}{Column name in the target dataset (required).}
\item{\code{label}}{Variable label.}
\item{\code{type}}{\code{"Char"} / \code{"C"} / \code{"character"} or \code{"Num"} / \code{"N"} / \code{"numeric"}.}
\item{\code{length}}{Byte length for character variables.}
\item{\code{format}}{SAS format string, e.g. \code{"DATE9."}, \code{"$CHAR200."}.}
\item{\code{dataset}}{Dataset name (optional; used to filter when one spec
file covers multiple domains).}
}
}
\examples{
\dontrun{
spec <- import_spec("SDTM_spec.xlsx", sheet = "DM", dataset = "DM")
dm   <- apply_spec(dm_raw, spec)
}

}
\seealso{
\code{\link[=apply_spec]{apply_spec()}}, \code{\link[=get_metadata]{get_metadata()}}
}
